echo "# veer-repo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/chethanhc1993/veer-repo.git
git push -u origin main

git config --global user.name"name"
git config --global user.email"email id"

